{
  "name": "taekion-project-api",
  "version": "1.0.0",
  "description": "API for Taekion project",
  "main": "dist/main.js",

  "dependencies": {
    "@nestjs/cli": "^10.4.5",
    "@nestjs/common": "^10.4.4",
    "@nestjs/core": "^10.4.4",
    "@nestjs/platform-express": "^10.4.4",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1"
  },
  "scripts": {
    "start": "nest start", 
    "start:dev": "nest start --watch",  
    "dev": "nest start --watch",
    "build": "tsc -p tsconfig.build.json",  
    "lint": "eslint \"{src,apps,libs,test}/**/*.ts\" --fix",
    "test": "jest"
  },
  "devDependencies": {
    "@types/node": "^22.7.5",
    "ts-node": "^10.9.2",
    "typescript": "^5.6.3"
  }
}
